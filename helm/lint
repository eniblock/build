#!/bin/sh

set -e

helm template test-release "$@" > /tmp/out.yaml

cat /tmp/out.yaml | kubeval --strict --schema-location file:///
cat /tmp/out.yaml | kube-linter lint --config /etc/kube-linter.yaml -
